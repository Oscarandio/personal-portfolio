---
import "devicon/devicon.min.css";

import { getLangFromUrl, useTranslations } from "../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

import { SKILLS } from "@/consts/skills";

const firstCard = SKILLS.slice(0, 8);
const secondCard = SKILLS.slice(8, 16);
const thirdCard = SKILLS.slice(16, 22);
const fourthCard = SKILLS.slice(22, 27);
const fifthCard = SKILLS.slice(27, 32);
const sixthCard = SKILLS.slice(32, 38);
---

<section class='w-full lg:w-8/12 2xl:w-6/12 mx-auto px-4'>
  <!-- <h2>Toolkit</h2> -->
  <div class='grid grid-cols-1 lg:grid-cols-2 gap-4'>
    <article
      class='bg-transparent-white border border-gray-300 rounded-lg pt-3 pb-4 px-5'
    >
      <header>
        <h2 class='font-medium text-xl'>{t("skills.languages")}</h2>
      </header>
      <ul class='pt-4'>
        {
          firstCard.map(({ id, name }) => (
            <li>
              <i
                class={`devicon-${id}-plain ${id === "less" ? `devicon-${id}-plain-wordmark colored` : `devicon-${id}-plain colored`}`}
              />
              {name}
            </li>
          ))
        }
      </ul>
    </article>
    <article
      class='bg-transparent-white border border-gray-300 rounded-lg pt-3 pb-4 px-5'
    >
      <header>
        <h2 class='font-medium text-xl'>{t("skills.frameworks")}</h2>
      </header>
      <ul class='pt-4'>
        {
          secondCard.map(({ id, name }) => (
            <li>
              <i class={`devicon-${id}-plain colored`} />
              {name}
            </li>
          ))
        }
      </ul>
    </article>

    <article
      class='bg-transparent-white border border-gray-300 rounded-lg pt-3 pb-4 px-5'
    >
      <header>
        <h2 class='font-medium text-xl'>Backend & Runtime</h2>
      </header>
      <ul class='pt-4'>
        {
          thirdCard.map(({ id, name }) => (
            <li>
              <i
                class={`devicon-${id}-plain ${id === "express" || "mongoose" ? `devicon-${id}-original colored` : `devicon-${id}-plain colored`}`}
              />
              {name}
            </li>
          ))
        }
      </ul>
    </article>
    <article
      class='bg-transparent-white border border-gray-300 rounded-lg pt-3 pb-4 px-5'
    >
      <header>
        <h2 class='font-medium text-xl'>Build Tools & Bundlers</h2>
      </header>
      <ul class='pt-4'>
        {
          fourthCard.map(({ id, name }) => (
            <li>
              <i
                class={`devicon-${id}-plain ${id === "npm" ? `devicon-${id}-original-wordmark colored` : `devicon-${id}-plain colored`}`}
              />
              {name}
            </li>
          ))
        }
      </ul>
    </article>
    <article
      class='bg-transparent-white border border-gray-300 rounded-lg pt-3 pb-4 px-5'
    >
      <header>
        <h2 class='font-medium text-xl'>{t("skills.clouds")}</h2>
      </header>
      <ul class='pt-4'>
        {
          fifthCard.map(({ id, name }) => (
            <li>
              <i
                class={`devicon-${id}-plain ${id === "notion" ? "" : "colored"}`}
              />
              {name}
            </li>
          ))
        }
      </ul>
    </article>
    <article
      class='bg-transparent-white border border-gray-300 rounded-lg pt-3 pb-4 px-5'
    >
      <header>
        <h2 class='font-medium text-xl'>{t("skills.tools")}</h2>
      </header>
      <ul class='pt-4'>
        {
          sixthCard.map(({ id, name }) => (
            <li>
              <i
                class={`devicon-${id}-plain ${id === "notion" ? "" : "colored"}`}
              />
              {name}
            </li>
          ))
        }
      </ul>
    </article>
  </div>
</section>
<style>
  ul {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    list-style: none;
    margin-block: 0;
  }

  li {
    background-color: rgb(255 255 255 / 85%);
    display: flex;
    align-items: center;
    color: var(--text-color-bold);
    font-size: 0.9rem;
    width: fit-content;
    padding: 6px 12px;
    border-radius: 6px;
  }
  i {
    font-size: 1.4rem;
    margin-right: 0.3rem;
  }
</style>
